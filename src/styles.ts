import { makeStyles } from "tss-react/mui";

export const useStyles = makeStyles()(() => ({
  root: {
    padding: "15px",
    display: "flex",
    height: "95vh",
    alignItems: "center",
    justifyContent: "center",
  },
}));
